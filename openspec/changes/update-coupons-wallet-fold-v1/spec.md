# Spec: update-coupons-wallet-fold-v1

## Summary
将“我的券包”页面按券类型折叠展示（同类券显示 `×N`），并确保一次核销只消耗 1 张券（优先核销最早获得的未使用券）。

## Scope
- 券包页按 `title + desc` 对券进行分组折叠展示。
- 分组卡片展示：名称、描述、最新获得时间、状态（未使用/已使用）、数量（`×N`）。
- 分组存在未使用券时展示“标记为已使用”按钮，点击后仅核销 1 张券。

## Out of scope
- 不修改云函数与数据库结构。
- 不新增券详情页或复杂筛选。
