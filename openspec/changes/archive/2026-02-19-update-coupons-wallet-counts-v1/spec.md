# Spec: update-coupons-wallet-counts-v1

## Summary
在券包折叠分组卡片中增加“已获得/已使用/还剩”统计行，帮助用户快速理解同类券的使用进度。

## Scope
- 券包页对每个分组卡片展示：`已获得 X 张 · 已使用 Y 张 · 还剩 Z 张`。
- 统计数来自同组券实例的状态：
  - `已获得 = 总数`
  - `已使用 = used 数`
  - `还剩 = unused 数`

## Out of scope
- 不修改后端接口与券实例字段。
- 不改变现有分组规则、状态 badge 规则与核销逻辑。
